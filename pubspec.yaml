name: elsf
description: Emergency Local Services Finder - A real-world production Flutter app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # core
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  go_router: ^13.2.0
  # shared_preferences: ^2.2.2
  
  # firebase
  # firebase_core: ^2.27.0
  # firebase_auth: ^4.17.8
  # cloud_firestore: ^4.15.8
  # firebase_messaging: ^14.7.19
  # cloud_functions: ^4.6.0
  
  # ui & animations
  google_fonts: ^6.1.0
  flutter_animate: ^4.5.0
  cached_network_image: ^3.3.1
  flutter_svg: ^2.0.10
  shimmer: ^3.0.0
  
  # maps & location
  # google_maps_flutter: ^2.6.0
  # geolocator: ^11.0.0
  
  # utils
  uuid: ^4.3.3
  intl: ^0.19.0
  logger: ^2.0.2
  rxdart: ^0.27.7
  google_maps_flutter: ^2.14.0
  geolocator: ^14.0.2
  geocoding: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  riverpod_generator: ^2.4.0
  build_runner: ^2.4.8
  custom_lint: ^0.6.4
  riverpod_lint: ^2.3.10

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
